## Setup

1. Clone the repository
2. In `app.js` and `ai_test_runner.html` replace `YOUR_DUKE_API_TOKEN` with your DukeGPT API token

## Note
This project uses DukeGPT (GPT 4.1 Mini via Duke OIT LiteLLM). 
Access requires a valid Duke University API token.